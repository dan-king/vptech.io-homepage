# README.md

# Example of invoking a CKAN data catalog via a WSO2 API gateway

### Install
`$ npm install`

### Configure
`$ cp config.template.json config.json`
`$ vi config.json`

### Run
`$ node index.js`
